package com.netos;

import cj.netos.network.peer.IPeer;
import cj.netos.network.peer.Peer;

public class TestPeer {
    public static void main(String... args) {
        connect();
    }

    private static void connect() {
//        IPeer peer= Peer.connect("",);
    }
}
